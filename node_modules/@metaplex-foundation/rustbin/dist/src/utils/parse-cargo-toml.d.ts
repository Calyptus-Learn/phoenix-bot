/** @private */
export type CargoToml = {
    dependencies: Record<string, string | {
        version: string;
    }>;
};
/** @private */
export declare function parseCargoToml(fullPath: string): Promise<{
    parsed: CargoToml;
    toml: string;
}>;
